apiVersion: v1
kind: Service
metadata:
  name: gbp-backend
  namespace: gbp
  labels:
    app: gbp-backend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: gbp-backend
