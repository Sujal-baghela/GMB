apiVersion: v1
kind: Service
metadata:
  name: gbp-frontend
  namespace: gbp
  labels:
    app: gbp-frontend
spec:
  type: ClusterIP
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: gbp-frontend
